# Development / test dependencies.
#
# Install on top of the application dependencies:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# NOTE (Python 3.14): the versions pinned in requirements.txt predate cp314
# wheels for pydantic-core, so they require a C/Rust toolchain to build from
# source. On a machine without a compiler, install current wheel-backed
# versions instead (see docs/testing.md "Troubleshooting").

pytest>=8.3
pytest-cov>=5.0
pytest-asyncio>=0.24
# Required by Starlette's TestClient.
httpx>=0.27

# --- Dependency vulnerability auditing -------------------------------------
# pip-audit usa a base de advisories do PyPA/OSV e funciona bem nas versões
# recentes do Python.
#     pip-audit                         # audita o ambiente instalado
#     pip-audit -r requirements.txt -r requirements-dev.txt
pip-audit>=2.7
#
# Alternativa: `safety check`. O `safety` pode não suportar imediatamente as
# versões mais novas do Python (ex.: 3.14); nesse caso, use o pip-audit acima
# (já coberto pelo job `dependency-audit` do CI) ou rode o safety num ambiente
# Python 3.12:
#     pip install safety && safety check -r requirements.txt
