[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    integration: integration tests using the full app stack and an isolated DB
    mysql_only: only runs against MySQL (TEST_DATABASE_URL); skipped on SQLite
filterwarnings =
    ignore::DeprecationWarning
